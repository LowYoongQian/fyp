fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
# Schema changes go through Alembic only - never hand-written ALTER TABLE at startup.
alembic==1.13.2
psycopg[binary]
google-generativeai==0.7.2
python-dotenv==1.0.1
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.2.1
httpx==0.27.0
bcrypt==4.1.3
# Test runner for tests/ (see pytest.ini). Not imported by the app itself.
pytest==9.1.1

# Face recognition (real embedding extraction + cosine matching)
# Versions below are the ones actually verified working on Python 3.11 - the
# previous tensorflow==2.19.1 / tf-keras==2.19.0 pins were never installed and
# pull in numpy 2.x, which breaks the deepface backend at import time.
deepface==0.0.100
tensorflow==2.14.0
tf-keras==2.15.0
opencv-python==4.11.0.86

# Analytics / ML dependencies (at-risk prediction model)
# numpy pinned <2.0: TensorFlow 2.14 (deepface backend) is incompatible with
# numpy 2.x. scikit-learn 1.9.0 (risk_model.pkl) only needs numpy>=1.24.1, and
# pandas 2.2.x supports numpy 1.26 - so this pin keeps face + risk modules working.
# scikit-learn MUST stay 1.9.0: ml/risk_model.pkl was pickled with it, and
# unpickling across a different minor version is not guaranteed.
numpy==1.26.4
scikit-learn==1.9.0
pandas==2.2.3
joblib==1.5.3