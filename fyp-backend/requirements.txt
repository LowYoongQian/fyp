fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
psycopg[binary]
google-generativeai==0.7.2
python-dotenv==1.0.1
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.2.1
httpx==0.27.0
bcrypt==4.1.3

# Face recognition (real embedding extraction + cosine matching)
deepface==0.0.100
tensorflow==2.19.1
tf-keras==2.19.0

# Analytics / ML dependencies (at-risk prediction model)
# numpy pinned <2.0: TensorFlow 2.14 (deepface backend) is incompatible with
# numpy 2.x. scikit-learn 1.9.0 (risk_model.pkl) only needs numpy>=1.24.1, and
# pandas 2.2.x supports numpy 1.26 — so this pin keeps face + risk modules working.
numpy==2.1.3
scikit-learn==1.7.2
pandas==3.0.3
joblib==1.5.3